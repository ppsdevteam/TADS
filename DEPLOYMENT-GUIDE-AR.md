# حزمة موقع TADS النهائية

هذه الحزمة مطابقة لآخر نسخة منشورة من الموقع، وتشمل كود المصدر، الصور، الشعارات، وملفات الإنتاج الجاهزة داخل مجلد `dist`.

## محتويات الحزمة

- `app/`: صفحات الموقع والمحتوى والتصميم.
- `public/`: جميع الصور والشعارات والملفات العامة.
- `dist/`: نسخة الإنتاج المبنية والجاهزة لاستضافة تدعم Cloudflare Workers.
- `package.json` و`package-lock.json`: الاعتماديات وأوامر التشغيل.
- ملفات الإعداد والبناء والاختبارات.

لا تتضمن الحزمة `node_modules` أو سجل Git أو ملفات الكاش المحلية، لتبقى خفيفة وآمنة للنقل.

## المتطلبات

- Node.js إصدار `22.13.0` أو أحدث.
- npm.
- استضافة تدعم Node.js أو Cloudflare Workers.

## تشغيل الموقع من كود المصدر

من داخل مجلد الموقع:

```bash
npm ci
npm run build
npm run start
```

بعد التشغيل، اربط الدومين بالموقع من لوحة الاستضافة أو عبر Reverse Proxy مثل Nginx.

## النشر على Cloudflare Workers — الخيار الموصى به

1. سجّل الدخول إلى Cloudflare من سطر الأوامر:

```bash
npx wrangler login
```

2. انشر نسخة الإنتاج الموجودة داخل الحزمة:

```bash
npx wrangler deploy --config dist/server/wrangler.json
```

3. من لوحة Cloudflare افتح المشروع، ثم أضف الدومين من قسم **Custom Domains**.

## ملاحظة عن cPanel والاستضافة المشتركة

رفع الملفات مباشرة داخل `public_html` لا يكفي إذا كانت الاستضافة تدعم ملفات HTML ثابتة فقط. يجب أن تدعم الاستضافة Node.js، أو يتم النشر عبر Cloudflare Workers. إذا زوّدت مطوّر الموقع باسم شركة الاستضافة ونوع الخطة، يمكن تحديد خطوات الرفع وربط الدومين بدقة.

## الدومين

لا يحتاج ربط الدومين إلى تعديل داخل كود الموقع. يتم الربط من لوحة شركة الدومين أو الاستضافة عبر سجلات DNS أو ميزة Custom Domains.
